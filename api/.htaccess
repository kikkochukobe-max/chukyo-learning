# config.php は本来 web/ の外（DOCUMENT_ROOTの兄弟フォルダ）に置く想定。
# FTPの制約で外に出せずここに置く場合の保険として、直接アクセスを拒否する。
<Files "config.php">
  <IfModule mod_authz_core.c>
    Require all denied
  </IfModule>
  <IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
  </IfModule>
</Files>
