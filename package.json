{
  "name": "chukyo-learning-tests",
  "private": true,
  "description": "学習ツールの回帰テスト用（本番へは配信しない。Hetemlへは learning/ と assets/ だけをアップする）",
  "scripts": {
    "test": "playwright test",
    "test:hyakumasu": "playwright test tests/hyakumasu-fastkey.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
